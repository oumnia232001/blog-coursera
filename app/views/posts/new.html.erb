<% content_for :title, "New post" %>

<div class="container mt-5">
  <h1 class="text-center mb-4">New post</h1>

  <%= render "form", post: @post %>

  <div class="text-center mt-4">
    <%= link_to "Back to posts", posts_path, class: "btn btn-primary btn-lg", style: "background-color: #007bff; color: white; padding: 8px 2px; border-radius: 5px; transition: background-color 0.3s;" %>
  </div>
</div>
